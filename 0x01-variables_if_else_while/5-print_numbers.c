#include <stdio.h>
/**
 * main - prints single base 10 digits starting from 0 
 * Return: Always 0 (Success)
 */

int main(void)
{
	int c = 0;

	while (c < 10)
	{
		printf("%d", c);
		c++;
	}
printf("\n");
return (0);
}
