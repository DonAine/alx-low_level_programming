#include "main.h"
/**
 * add - outputs sum of 2 numbers
 * Return: addition of the two input args
 * @a: input arg
 * @b: input arg
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
